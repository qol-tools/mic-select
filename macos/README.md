# macOS / Raycast

Install: `brew install switchaudio-osx && pip install -r requirements.txt && cd raycast && npm install && npm run build`

Usage: Open Raycast (`Cmd+Space`), search "List Microphones" or "Switch Microphone".

Requirements: Python 3.8+, Raycast, SwitchAudioSource
